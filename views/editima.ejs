<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editando</title>
    <style>
        body {
    padding: 50px;
    font: 14px "Lucida Grande", Helvetica, Arial, sans-serif;
  }
  
  a {
    color: #f700ff;
  }
  form{
    display: flex;
    flex-direction: column;
    gap: 5px;
    position: relative;
  }
  .input-wrapper{
    background-color: #d9e3dd;
    position: absolute;
    left: 20px;
    top: 10px;
    padding: 0 2px;
    transition: ease-in-out .1s;
    pointer-events: none;
  }
  input,
  textarea{
    background-color: transparent;
    padding: 12px 20px;
    border: 1px solid #4d4d4d;
    border-radius: 20px;
    font-size: 16px;
    outline: none;
    font-family: 'Poppins' sans-serif;
  }
  input:focus+label{
    top: -12px;
  }
    </style>
</head>
<body>

    <form action="/editima" method="post">
        <input type="hidden" value="<%= imagen.id %>" name="id">
        <label for="url">Url</label>
        <input type="text" name="url" value="<%= imagen.url %>"
        placeholder="Nueva Url" required>
        <label for="producto_id">Producto ID</label>
        <input type="text" name="producto_id" value="<%= imagen.producto_id %>">
        <label for="destacado">Destacado</label>
        <input type="text" name="destacado" value="<%= imagen.destacado %>">
        <input type="submit" value="Editar">


    </form>

</body>
</html>